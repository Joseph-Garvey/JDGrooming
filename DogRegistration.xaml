<UserControl x:Class="JDGrooming.DogRegistration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:JDGrooming"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <Border Background="#1e1e1e">
        <Viewbox>
            <StackPanel>
                <StackPanel Style="{StaticResource HorizontalCentred}">
                    <StackPanel VerticalAlignment="Bottom">
                        <Label Style="{StaticResource lbl_WhiteHeading}">Dog Name:</Label>
                        <TextBox></TextBox>
                        <Label Style="{StaticResource lbl_WhiteHeading}">Client Name:</Label>
                        <TextBox></TextBox>
                        <Label Style="{StaticResource lbl_WhiteHeading}">Breed:</Label>
                    </StackPanel>
                    <Border Margin="10" Style="{StaticResource brd_1}">
                        <StackPanel VerticalAlignment="Center">
                            <Border Style="{StaticResource brd_1}">
                                <Image Source="https://upload.wikimedia.org/wikipedia/commons/thumb/f/f0/Mops_oct09_cropped2.jpg/330px-Mops_oct09_cropped2.jpg" MaxHeight="68" MaxWidth="80"></Image>
                            </Border>
                            <Button Style="{StaticResource btnDark}" FontSize="6" Margin="5,0,5,5">Upload new image</Button>
                        </StackPanel>
                    </Border>
                </StackPanel>
                <ComboBox FontSize="6" Height="15" x:Name="cmb_Breed">
                    <ComboBoxItem>Pug</ComboBoxItem>
                </ComboBox>
                <Label Style="{StaticResource lbl_WhiteHeading}">Date of Birth:</Label>
                <DatePicker Name="dp_DOB" SelectedDateChanged="dp_DOB_SelectedDateChanged"/>
                <Label Style="{StaticResource lbl_WhiteHeading}">Additional Info:</Label>
                <TextBox></TextBox>
                <Button Style="{StaticResource btnDark}" Margin="10">Register</Button>
            </StackPanel>
            <!--
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Image Margin="10" Grid.Column="1" Source="https://upload.wikimedia.org/wikipedia/commons/thumb/f/f0/Mops_oct09_cropped2.jpg/330px-Mops_oct09_cropped2.jpg" Height="80" Width="80"></Image>
                <StackPanel>
                    <Label Style="{StaticResource lbl_WhiteHeading}">Dog Name:</Label>
                    <TextBox></TextBox>
                    <Label Style="{StaticResource lbl_WhiteHeading}">Client Name:</Label>
                    <TextBox></TextBox>
                </StackPanel>
                <StackPanel Grid.ColumnSpan="2" Grid.Row="1">
                    <Label Style="{StaticResource lbl_WhiteHeading}">Breed:</Label>
                    <TextBox></TextBox>
                    <Label Style="{StaticResource lbl_WhiteHeading}">Date of Birth:</Label>
                    <TextBox></TextBox>
                    <Label Style="{StaticResource lbl_WhiteHeading}">Additional Info:</Label>
                    <TextBox></TextBox>
                    <Button Style="{StaticResource btn}">Register</Button>
                </StackPanel>
            </Grid> -->
        </Viewbox>
    </Border>
</UserControl>
